<#include "/java_copyright.include">
<#assign className = clazz.className>
<#assign classNameLower = className?uncap_first>
package ${basepackage}.${subpackage}.service.${namespace};

import  ${basepackage}.${subpackage}.entity.${namespace}.${className};
import net.ptnetwork.service.BaseService;
<#include "/custom.include"/>

public interface ${className}Service extends BaseService<${className}, Long> {
	
}
